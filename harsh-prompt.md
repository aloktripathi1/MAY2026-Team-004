# Prompts

## 2026-07-10

Before we begin, for each prompt i give you , save it to prompt.md file at root please. Keep it under harsh-prompt.md. Save this one as well

---
YOUR WEEK - there is no spacing between the cards, all stick to each other fix that. Do not break existing code or functionality. Only work on given task.

---

When user switches from dashboard to other tab like Events, browse clubs, my issues , faq, profile, the Dashboard still remains hilighted with yellow color do and icon color. Fix this. Do not break existing code or functionality. Only work on given task.

---

In Profiles tab, under Notification preferences section , make the toggle working.Do not break existing code or functionality. Do not break existing code or functionality. Only work on given task.

---

In profile tab The border hilight still have black parts (very little) but entire thing should be of same colour. fix it please.Do not break existing code or functionality. Only work on given task.

---

## 2026-07-11

When I click on the + Raise the issue button , it should open a popup modal with turning background blur, fix this please.

---

Now add a filter button beside the +Raise issue button, which allows user to filter based on tags. - folloe the @design.md for the design guide. Do not break exisiting code / functionality.

---

when I click on both of tags , both get hilighted , at one time only one should, single category will be select, not all

---

convert filtering on basis of these as well (Priority + Status pills), like add a lower section with tags pill in Filter dropdown and user can select multiple and which one will be there , it will show up.

---

Looks good but I guess between Priority section and Status section,there can be a little spacing

---

When one click on edit modal , a popup with blur background should appear that allows user to edit Name, Interests. Make sure the popup is responsive. Reference the @design.md for the design guide for this. Do not break exisitng code / functionality.

---

yes let's do it (profile follow-ups: sync name cookie after edit + seed default interests). Do not break exisitng code / functionality.

---

remove the priority filter support from the Filter tab

---

remove category filter support from the filter tab as well

---

only one pill can be selected.
---

#### Volunteer Dashboard

Add this tab as well. Use @design.md for design guide. Do not break exisiting code , functionality, ui or any other member dashboard. follow best practices for tech stack choosen

---

On clicking register, show "requested" with closed color; set default as not requested from seeded data. Place visible, non-pill category text centered between main text and register button with proper vertical alignment.

---

- Clarify what happens when the user clicks the register button and whether the event coordinator needs to approve the action.
- Address unintended modal height changes when clicking Register.
- Add smooth/subtle animation when switching to the "Past" tab to avoid abrupt transitions.
- Investigate and fix inconsistent sidebar scrolling behavior between "Past" and "Upcoming" tabs.
- Make sidebar tab transitions smoother.

---

My current My Task tab is not what is in the wireframe, so use @design.md for design guide. Do not break exisiting code , functionality, ui or any other member dashboard. follow best practices for tech stack choosen. Make sure data remain consistent across the roles. Like it should not be like - if user has done something in this Volunteer dashboard, its not reflecting when neeeded.

---

Add a new section called COMPLETED under ASSIGNED TO ME section and move all the Completed task there (ticked off). User can click to complete the task and same modal moves to COMPLETED section with subtle animation. Do not break existing code and functionality.

